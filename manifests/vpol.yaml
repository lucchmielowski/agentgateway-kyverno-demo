# apiVersion: envoy.kyverno.io/v1alpha1
# kind: ValidatingPolicy
# metadata:
#   name: policy-global-allow
# spec:
#   evaluation:
#     mode: Envoy
#   matchConditions:
#   - name: request-body-contains-echo
#     expression: >
#       object.attributes.request.http.body.indexOf("echo") != -1
#   validations:
#   - expression:
#       envoy.Allowed().Response()
# ---
apiVersion: envoy.kyverno.io/v1alpha1
kind: ValidatingPolicy
metadata:
  name: policy
spec:
  evaluation:
    mode: Envoy
  validations:
  - expression:
      envoy.Allowed().Response()
      #envoy.Denied(403).Response()